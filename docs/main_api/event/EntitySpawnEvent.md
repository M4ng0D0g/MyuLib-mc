# EntitySpawnEvent

## Role
Primary type in `main.api.event` module.

## Responsibilities
- provide module-level behavior for `EntitySpawnEvent`
- integrate with sibling API types where applicable

## Example
```java
// Example for EntitySpawnEvent
EntitySpawnEvent value = null;
```
