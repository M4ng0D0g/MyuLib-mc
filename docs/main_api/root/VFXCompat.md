# VFXCompat

## Role
Primary type in `main.api.root` module.

## Responsibilities
- provide module-level behavior for `VFXCompat`
- integrate with sibling API types where applicable

## Example
```java
// Example for VFXCompat
VFXCompat value = null;
```
