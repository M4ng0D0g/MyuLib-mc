# CameraTrackingTarget

## Role
Primary type in `main.api.camera` module.

## Responsibilities
- provide module-level behavior for `CameraTrackingTarget`
- integrate with sibling API types where applicable

## Example
```java
// Example for CameraTrackingTarget
CameraTrackingTarget value = null;
```
