# CameraCompat

## Role
Primary type in `client.api.camera` module.

## Responsibilities
- provide client-side behavior for `CameraCompat`
- integrate with client runtime loop and UI/render/input paths

## Example
```java
// Example for CameraCompat
CameraCompat value = null;
```
