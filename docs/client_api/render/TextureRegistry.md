# TextureRegistry

## Role
Primary type in `client.api.render` module.

## Responsibilities
- provide client-side behavior for `TextureRegistry`
- integrate with client runtime loop and UI/render/input paths

## Example
```java
// Example for TextureRegistry
TextureRegistry value = null;
```
