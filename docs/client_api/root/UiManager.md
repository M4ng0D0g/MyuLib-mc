# UiManager

## Role
Primary type in `client.api.root` module.

## Responsibilities
- provide client-side behavior for `UiManager`
- integrate with client runtime loop and UI/render/input paths

## Example
```java
// Example for UiManager
UiManager value = null;
```
